<h1>AFL Database</h1>
<p>For the 2021 AFL season</p>

<table class="model-schema">

  <thead>
    <tr>
      <th>Model</th>
      <th>Path</th>
    </tr>
  </thead>

  <tbody>
    <% get_all_models.each do |model| %>
      <tr>
        <td><%= model %></td>
        <td><%= link_to "/#{model.downcase}s", "/#{model.downcase}s" %></td>
      </tr>
    <% end %>
  </tbody>

</table>
