<h3 class="breadcrumbs">
  <%= link_to 'AFL Database', root_path %>
  /
  <%= link_to 'Fixtures', fixtures_path %>

  <%= " / " if @round.present? %>
  <%= link_to @round, round_path(@round) if @round.present? %>

  <% if @fixture.present? %>
    <%= " / " %>
    <%= link_to @fixture.short_name, fixture_path(@round, @fixture.id) %>
  <% end %>

</h3>
